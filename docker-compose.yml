version: '3.1'

services:
  db:
    image: postgres
    ports:
      - "5432:5432"
    restart: always
    environment:
      POSTGRES_DB: security_example
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
